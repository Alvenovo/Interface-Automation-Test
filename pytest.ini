[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts =
    -v
    --strict-markers
    --alluredir=allure-results
    --tb=short

markers =
    smoke: 冒烟测试
    regression: 回归测试
    slow: 耗时较长的测试
    api: 接口测试
